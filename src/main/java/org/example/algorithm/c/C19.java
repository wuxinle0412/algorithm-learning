package org.example.algorithm.c;

/**
 *  将单向链表按某值划分成左边小、中间相等、右边大的形式
 *
 *  给定一个单向链表的头节点head，节点的值类型是整型，再给定一个整数pivot。实现一个调整链表的函数，将链表调整为
 *  左部分都是小于pivot的节点，中间部分都是值等于pivot的节点，右部分都是值大于pivot的节点。除这个要求外，对调整
 *  后的节点顺序没有更多的要求。
 *
 *  进阶问题:
 *  在左、中、右三个部分的内部也做顺序要求，要求每部分里的节点从左到右的顺序与原链表的先后次序一致。
 *
 * */
public class C19 {
}
